# Netlify detects Next.js and applies its runtime (OpenNext adapter)
# automatically; this just pins the build.
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
